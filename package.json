{
  "name": "license-generator",
  "version": "1.0.0",
  "description": "License Agreement Generator with AI Chatbot",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "backend-mac": "cd backend && source venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
} 